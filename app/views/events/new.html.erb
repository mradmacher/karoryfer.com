<%= render_title t( 'helpers.title.event.new' ) %>
<h2><%= t 'helpers.title.event.new' %></h2>
<p>
  <a href="<%= artist_path(@event.artist) %>" class="btn btn-warning btn-xs"><%=t 'helpers.action.cancel_new' %></a>
</p>
<%= render :partial => 'form' %>
