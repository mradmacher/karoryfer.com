<%= render_title t( 'helpers.title.event.edit' ) %>
<% content_for :context_actions do %>
  <%= link_to_action t( 'helpers.action.cancel_edit' ), artist_event_path( @event.artist, @event ) %>
<% end %>
<h1><%= current_artist.name %></h1>
<h2><%= t 'helpers.title.event.index'  %></h2>
<h3><%= t 'helpers.title.event.edit' %></h3>
<%= render :partial => 'form' %>
