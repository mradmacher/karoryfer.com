<hr />
<div class="container">
  <div class="row">
    <div class="col-md-4">
      <ul class="list-inline">
        <% if current_user.can_manage_account? -%>
          <li><a href="<%= current_user.logout_path %>" class="btn btn-default btn-xs">Pa</a></li>
          <li><a href="<%= current_user.account_path %>" class="btn btn-default btn-xs">Moje konto</a></li>
        <% else -%>
          <li><a href="<%= current_user.login_path %>" class="btn btn-default btn-xs">Zaloguj się</a></li>
        <% end %>
        <% if current_user.can_manage_users? %>
          <li><a  href="<%= admin_users_path %>" class="btn btn-default btn-xs"><%= t('title.user.index') %></a></li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
