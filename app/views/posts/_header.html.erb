<h1><%= current_artist.name %></h1>
<h2><%= t 'helpers.title.post.index' %>
	<% unless post.published? %>
		<span class="status">(<%= t('helpers.label.post.unpublished') %>)</span>
	<% end %>
</h2>

<div class="media">
  <div class="media-body">
    <h3 class="media-heading"><%= post_icon %><%= post.title %></h3>
  </div>
</div>
<br />

