<%= render_title t( 'helpers.title.video.edit' ) %>
<% content_for :context_actions do %>
  <%= link_to_action t( 'helpers.action.cancel_edit' ), artist_video_path( @video.artist, @video ) %>
<% end %>
<h2 class="hidden"><%= t 'helpers.title.video.index'  %></h2>
<h3><%= @video.title %></h3>
<%= render :partial => 'form' %>

