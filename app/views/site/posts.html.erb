<%= render_title t( 'title.post.index' ) %>

<%= render partial: 'sidebar' %>
<%= render partial: 'nav' %>

<h1 class="hidden"><%= t 'title.post.index' %></h1>

<% if @posts.empty? %>
  <%= t( 'label.nothing_here_yet' ) %>
<% end %>
<ul class="media-list">
<% @posts.each do |post| %>
  <li class="media img-rounded">
    <div class="media-body">
      <h5><%= post_icon %><%= link_to post.title, artist_post_path( post.artist, post ) %></h5>
      <p><%= post.artist.name %></p>
    </div>
  </li>
<% end %>
</ul>

<%= render :partial => 'events/calendar_js' %>

