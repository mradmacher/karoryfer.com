<%= render_title @presenter.title %>
<h2><%= @presenter.title %></h2>
<p>
  <a href="<%= artist_page_path(current_artist, @presenter.resource) %>" class="small button"><%=t 'action.cancel_edit' %></a>
</p>
<%= render partial: 'form', locals: { resource: @presenter.resource } %>

