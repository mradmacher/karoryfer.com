<%= render_title t( 'helpers.title.album.edit' ), @album.title %>
<h2><%= @album.title %></h2>
<p>
  <a href="<%= artist_album_path(@album.artist, @album) %>" class="btn btn-warning btn-xs"><%=t 'helpers.action.cancel_edit' %></a>
</p>
<%= render :partial => 'form' %>

