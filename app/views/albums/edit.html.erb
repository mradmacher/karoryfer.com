<%= render_title t( 'helpers.title.album.edit' ), @album.title %>
<h2 class="hidden"><%= t 'helpers.title.album.index'  %></h2>
<h3><%= @album.title %></h3>
<% content_for :context_actions do %>
  <%= link_to_action t( "helpers.action.cancel_edit" ), artist_album_path( @album.artist, @album ) %>
<% end %>
<%= render :partial => 'form' %>

