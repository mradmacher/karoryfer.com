<%= render_title t( 'helpers.title.page.edit' ) %>
<h2><%= @page.title %></h2>
<p>
  <a href="<%= artist_page_path(@page.artist, @page) %>" class="btn btn-warning btn-xs"><%=t 'helpers.action.cancel_edit' %></a>
</p>
<%= render :partial => 'form' %>

