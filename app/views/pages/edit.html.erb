<%= render_actions :page, :cancel_edit => artist_page_path( current_artist, @page ) %>
<%= render_title t( 'helpers.title.page.edit' ) %>
<h1><%= @page.artist.name %></h1>
<h2><%= t 'helpers.title.page.index'  %></h2>
<h3><%= t( 'helpers.title.page.edit' ) %></h3>
<%= render :partial => 'form' %>

