<%= render_title t( 'helpers.title.page.new' ) %>
<% content_for :context_actions do %>
  <%= link_to_action t( "helpers.action.cancel_new" ), artist_path( @page.artist ) %>
<% end %>
<h1><%= @page.artist.name %></h1>
<h2><%= t( 'helpers.title.page.index' ) %></h2>
<h3><%= t( 'helpers.title.page.new' ) %></h3>
<%= render :partial => 'form' %>

